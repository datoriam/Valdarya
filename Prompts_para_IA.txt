Prompt 1 """"""""""" "A biblioteca libGDX foi usada e a partir dela criada o escopo para o projeto de um RPG em Java. Basta apenas colocar a pasta criada no Github para ser o esqueleto do projeto?". Nesse prompt, anexamos o arquivo "COMP0395___Projeto.pdf". """"""""""""""""""""""""""""""""""""" Resposta """"""""""" "Inicialize o Git corretamente: Não use a função de 'upload files' do site do GitHub. Use o terminal ou um cliente Git (git init, git add ., git commit). Verifique o .gitignore: Garanta que pastas como /build, /.gradle e /.idea ou /.settings estejam ignoradas. Teste o Clone: Antes de avisar o grupo, tente clonar o repositório em outra pasta e rodar o comando de execução (ex: gradlew desktop:run) para garantir que ele roda em um ambiente limpo. Divida Tarefas: Como o grupo é de até 4 pessoas, dividam quem vai criar a classe Combatente, quem vai fazer a Arena, etc., para que todos tenham commits." """""""""""""""""""""""""""""""""""""

Prompt 2 """"""""""" "Como podemos estruturar as subclasses Guardião, Arcanista e Atirador para que o método atacar() seja diferente para cada uma, usando polimorfismo e as mecânicas de Vigor e Mana?" """"""""""""""""""""""""""""""""""""" Resposta """"""""""" "Implemente a sobrescrita (Override) do método atacar() em cada subclasse. No Guardião, faça o ataque gerar Vigor para o bloqueio. No Arcanista, condicione o ataque ao gasto de Mana com multiplicadores mágicos. No Atirador, adicione chances de aplicar status como 'Sono'. Certifique-se de que a classe Batalha trate todos como a superclasse Combatente para que o Late Binding do Java decida qual ataque executar em tempo de execução." """""""""""""""""""""""""""""""""""""

Prompt 3 """"""""""" "O jogo está ficando muito difícil nos andares altos. Como podemos frear o crescimento (scaling) dos monstros Slime e Zumbi e garantir que o herói mude automaticamente quando um morrer?" """"""""""""""""""""""""""""""""""""" Resposta """"""""""" "Ajuste o método evoluirStats() dos inimigos para um crescimento linear mais suave (ex: +1 de dano em vez de +4). Na classe Batalha, crie o método trocarFocoHeroi() que percorre a ArrayList<Combatente> do time aliado em busca do próximo objeto com vida ativa. Chame essa função dentro do loop de ataque dos inimigos para evitar que o jogo tente interagir com uma instância derrotada e cause erros de lógica." """""""""""""""""""""""""""""""""""""
